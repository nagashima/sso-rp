<div class="container mx-auto px-4 py-8">
  <div class="flex justify-center">
    <div class="w-full max-w-4xl">
      <div class="bg-white shadow-lg rounded-lg overflow-hidden">
        <div class="bg-blue-500 text-white px-6 py-4 flex justify-between items-center">
          <h3 class="text-xl font-semibold">プロフィール情報</h3>
          <%= button_to "ログアウト", logout_path, method: :delete,
                        class: "bg-red-500 hover:bg-red-600 text-white font-medium py-2 px-4 rounded text-sm transition duration-200",
                        data: { confirm: "ログアウトしますか？" } %>
        </div>
        <div class="px-6 py-6">
          <% if @user_info %>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
              <div>
                <h5 class="text-lg font-medium text-gray-800 mb-4">基本情報</h5>
                <div class="space-y-3">
                  <div class="flex">
                    <dt class="font-medium text-gray-600 w-32">ユーザーID:</dt>
                    <dd class="text-gray-800"><%= @user_info['uid'] || @user_info[:uid] %></dd>
                  </div>
                  <div class="flex">
                    <dt class="font-medium text-gray-600 w-32">メールアドレス:</dt>
                    <dd class="text-gray-800"><%= @user_info['email'] || @user_info[:email] %></dd>
                  </div>
                  <div class="flex">
                    <dt class="font-medium text-gray-600 w-32">氏名:</dt>
                    <dd class="text-gray-800"><%= @user_info['name'] || @user_info[:name] %></dd>
                  </div>
                  <% if @user_info['birth_date'] %>
                  <div class="flex">
                    <dt class="font-medium text-gray-600 w-32">生年月日:</dt>
                    <dd class="text-gray-800"><%= @user_info['birth_date'] %></dd>
                  </div>
                  <% end %>
                  <% if @user_info['phone_number'] %>
                  <div class="flex">
                    <dt class="font-medium text-gray-600 w-32">電話番号:</dt>
                    <dd class="text-gray-800"><%= @user_info['phone_number'] %></dd>
                  </div>
                  <% end %>
                  <% if @user_info['address'] %>
                  <div class="flex">
                    <dt class="font-medium text-gray-600 w-32">住所:</dt>
                    <dd class="text-gray-800"><%= @user_info['address'] %></dd>
                  </div>
                  <% end %>
                </div>
              </div>

              <div>
                <h5 class="text-lg font-medium text-gray-800 mb-4">技術情報</h5>
                <div class="space-y-3">
                  <div class="flex">
                    <dt class="font-medium text-gray-600 w-32">認証方法:</dt>
                    <dd class="text-gray-800">OpenID Connect</dd>
                  </div>
                  <div class="flex">
                    <dt class="font-medium text-gray-600 w-32">IdP:</dt>
                    <dd class="text-gray-800">ORY Hydra + Rails IdP</dd>
                  </div>
                  <% if @user_info['access_token'] %>
                  <div class="flex">
                    <dt class="font-medium text-gray-600 w-32">アクセストークン:</dt>
                    <dd class="text-gray-800">
                      <code class="bg-gray-100 px-2 py-1 rounded text-sm font-mono truncate max-w-xs inline-block">
                        <%= @user_info['access_token'][0..20] %>...
                      </code>
                    </dd>
                  </div>
                  <% end %>
                </div>
              </div>
            </div>

            <div class="mt-8">
              <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded">
                <strong>SSO認証成功！</strong>
                IdPアプリケーションから取得した情報を表示しています。
              </div>
            </div>
          <% else %>
            <div class="bg-yellow-100 border border-yellow-400 text-yellow-700 px-4 py-3 rounded">
              ユーザー情報を取得できませんでした。
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>